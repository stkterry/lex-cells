
class ExtensibleFunction extends Function {
  constructor(f) {
    return Object.setPrototypeOf(f, new.target.prototype);
  }
}

class Vec2 extends ExtensibleFunction {
  constructor(x=0, y=0) {
    super(() => {return [this.x, this.y]});
    this.x = x;
    this.y = y;
  }
}

export default Vec2;